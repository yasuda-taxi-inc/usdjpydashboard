"""
wf_sr_config.py — サポート/レジスタンス(S/R)戦略の検証設計【凍結版】
作成: 2026-09-21。この時点で、成績(勝ち負け・期待値・方向別の平均)は一切見ていない。
見たのは、次の「成績を含まない」情報だけ:
  ・データの品質(時刻・継ぎ目・整合)
  ・イベントの件数と時刻の分布(wf_sr_events.py / enum_events.js / power_check.py)
  ・独立な観測の数と、最小検出効果(MDE)
この設定は、ハッシュを付けて凍結する。結果を見た後に変えてはならない。変える場合は、新しい版として、変更理由と共に別ファイルにする。

【この検証が答える問い】
  「S/Rの水準に、反発・ブレイク・偽ブレイクの反応が出たあと、その後の3時間の値動きに、
   往復コスト(0.4pips)を引いてなお残る偏りはあるか。その偏りは、水準の位置を無作為にずらしたプラセボと比べて、優位か」
"""
import hashlib, json

# ---------- データ(検査済み) ----------
DATA = {
    "5m": "USD_JPY_5min_2021-0101_2026-0915.csv", "1h": "USDJPY_1h_2021-0101-2026-0915.csv",
    "period": "2021-01-01〜2026-09-15(UTC)。判定は開始から30日後(2021-01-31)から",
    "weekend": "土曜と、金曜UTC22時以降は除く(2026年にだけ入っていた土日の足を、他の年と揃えるため)",
    "known_issues": [
        "2021〜2024年は価格が0.5pips刻みに丸められている(2025年以降は細かい)。コスト0.2pips(片道)より粗いため、ゾーン幅(約4pips)には影響しないが、結果の解像度は低い",
        "2026-01-10付近で、データ源が切り替わっている(土日の足が現れる)。値幅の時間帯プロファイルは連続(相関0.979)で、時刻はUTCで一貫",
    ],
}

# ---------- 実運用と同じ検出ロジックを使う(再実装しない) ----------
DETECTOR = "sr_core.js (SR_CORE.detect)。パラメータは、実運用の既定値のまま。検証で調整しない"
JUDGE_EVERY = "5分足12本(1時間)ごと"

# ---------- コスト ----------
COST_ONE_WAY_PIPS = 0.2          # 利用者の指定(SBI証券での実測値)
COST_ROUND_PIPS = 0.4            # 往復
COST_SENSITIVITY = [0.5, 1.5]    # 感度確認(0.5倍・1.5倍)。判定に使うのは、1.0倍のときだけ

# ---------- 取引の定義(すべて、イベント発生後に決まっていること) ----------
# 方向: 反応の種類から機械的に決める。成績を見て選ばない。
TRADE_RULES = {
    "rejected": "反発の方向へ入る。side='down'(レジスタンスで反発)は売り、side='up'(サポートで反発)は買い",
    "broken":   "ブレイクの方向へ入る。side='up'は買い、side='down'は売り(順張り)",
    "fake":     "偽ブレイクの逆方向へ入る。side='up'(上抜け失敗)は売り、side='down'(下抜け失敗)は買い",
}
ENTRY = "イベントを検出した確定足の、次の5分足の始値(=実運用で、通知を受けて動ける最短)"
HORIZON_BARS = 36                 # 3時間(5分足36本)。実運用の値幅予測と同じ
EXIT_MODE = "time"                # 主指標: 3時間後の終値で決済(TP/SLは使わない=探索の自由度を増やさない)
# 補助指標(主指標ではない。参考としてのみ報告): TP=1.0×ATR / SL=1.0×ATR の先着(同時到達はSL優先)
AUX_TP_SL_ATR = (1.0, 1.0)

# ---------- 分析の単位と、検定の自由度の制限 ----------
# 探索してよい切り口は、次の6つだけ(事前に固定)。これ以外の切り口(時間帯・強さ・種類の組合せ等)は、
# 探索的分析として別に報告してよいが、判定には使わない。
PRIMARY_CELLS = [
    ("rejected", "any"), ("rejected", "A"),
    ("broken",   "any"), ("broken",   "A"),
    ("fake",     "any"), ("fake",     "A"),
]
N_TESTS = len(PRIMARY_CELLS)      # 6。多重検定の補正(Bonferroni)に使う: 有意水準 0.05 / 6 = 0.0083

# ---------- 独立性の扱い ----------
INDEPENDENCE = "同じ切り口の中で、イベント間隔が3時間未満のものは、先に起きた方だけを残す(貪欲法)。窓が重なると同じ値動きを二重に数えるため"
CI_METHOD = "ブロックブートストラップ(ブロック=ISO週、10,000回)。イベントが同じ週に集まること、相場のレジームが持続することを反映する"

# ---------- 時間の分割(データが5.7年しかないため、ウォークフォワードの形を簡素にする) ----------
# 各切り口について「パラメータを学習する」部分がない(方向と決済は規則で固定)ため、
# 学習/テストの分割は不要。代わりに、年ごとの一貫性(符号と大きさ)と、前半/後半の安定性を見る。
YEAR_CONSISTENCY = "6年(2021〜2026。2026は途中)のうち、コスト後の平均が正の年が何年か。期待値の符号が揃うことを重視"
SPLIT_HALVES = ("2021-01〜2023-12", "2024-01〜2026-09")      # 前半・後半で、符号が一致するか

# ---------- プラセボ(最重要) ----------
# 「S/Rの水準に反応した」ことに意味があるのか、それとも「どこでも、ある価格に触れた後の動きは同じ」なのかを区別する。
# 各判定時点で、実際に検出された水準の中心を、無作為な量だけ上下にずらした「偽の水準」に置き換え、
# 同じ検出・同じ取引規則で、同じ件数のイベントを作り、同じ統計量を計算する。これを N_PLACEBO 回。
PLACEBO = {
    "method": "各水準の中心を、水準ごとに固定のずらし量(元の中心の価格とシードから決定的に決める)で移す。"
              "ずらし量の大きさは [0.5, 3.0]×0.21円(=約0.5〜3.0×ATR)の一様、符号は50%。ゾーン幅・強さ・種類・タッチ数は保持。"
              "ずらした位置で、反応の状態(rejected/broken/fake)を、同じ規則で再判定する。"
              "ずらし量を『時点ごとに引き直す』と、状態の遷移が追跡できず、イベントが激減して比較にならない(試験で確認: 実625件に対し36件)。だから水準ごとに固定する",
    "what_it_controls": "『水準の位置』の情報だけを壊す。時間帯の分布(実と差 0.085)・ATR(実10.6 / 偽11.1)は、ほぼ保たれる(試走で確認済み)",
    "known_limit": "イベント件数は実データより少ない(試走: 実625件に対し352件=56%)。水準が『価格が実際に反転した場所』から作られているため、"
                   "偽の位置ではイベントが起きにくいのは当然。判定は『1取引あたりの平均』で行い、件数差は直接は効かないが、"
                   "プラセボ分布のばらつきは実データより大きくなる(=判定は保守的になる)。件数の差そのものは、S/Rの効果の証拠として使わない",
    "N_PLACEBO": 500,
    "statistic": "各切り口の、コスト後の平均(pips/取引)。独立化(3時間未満の重複を除く)は、実データとプラセボで同じ手順を適用する",
    "pass": "実際の値が、プラセボ分布の上位2.5%(片側の有意水準0.025)に入ること。※方向は規則で固定しているので片側検定。"
            "切り口が6つあるので、6切り口すべてで検定する場合、Bonferroni補正(0.025/6≒0.0042)は、プラセボ500回では下限(1/500=0.002)に近く、"
            "検出力が不足する。したがって、プラセボ通過は『各切り口で上位2.5%』とし、多重検定の補正は、ブロックブートストラップ区間側(Bonferroni)で行う",
}

# ---------- 判定基準(3つの結論に分ける。結果を見た後に動かさない) ----------
# 事前の見積もり(power_check.py): 独立な観測は全体で約5,700件、3時間の値動きの標準偏差は約36pips。
# → 検出できる最小の平均効果(5%・検出力80%)は、全体で約1.35pips、強さAの反発で約1.97pips、偽ブレイクで約3.7pips。
# → 「有意でない」は、これらより小さい効果の否定にはならない。だから、結論を3つに分ける。
VERDICTS = {
    "A_エッジあり": "次のすべてを満たす切り口が、少なくとも1つある: "
                    "(1) コスト後の平均 > 0、Bonferroni補正後(0.05/6)のブロックブートストラップ95%区間(片側)が0を含まない "
                    "(2) プラセボの上位2.5%以内 "
                    "(3) 6年のうち4年以上で、コスト後の平均が正 "
                    "(4) 前半・後半の両方で、コスト後の平均が正 "
                    "(5) コスト1.5倍でも、コスト後の平均が正",
    "B_エッジなし(と言える)": "全ての切り口について、コスト後の平均の95%信頼区間の『上限』が、MDE(その切り口の最小検出効果)を下回る。 "
                             "=検出できる大きさの効果は、無いと言える。小さな効果(MDE未満)の存在は、否定できないと明記する",
    "C_判定不能": "AでもBでもない。=効果があるかどうか、このデータでは決められない。この場合、方向予測の実装には進まず、"
                  "『確率つきの参考情報』(第2希望)にも、検証済みとは書かない",
}
# 進路の対応(利用者の方針: 第一希望=方向を表示 / 第二希望=確率つきの参考情報)
ROUTES = {
    "A_エッジあり": "エッジのあった切り口に限って、方向の表示を検討する。未使用の期間(2026-09-16以降のライブ記録)で、再度、同じ基準で確認してから",
    "B_エッジなし(と言える)": "方向の表示は実装しない。水準の表示と、反応の状態の表示までにとどめる",
    "C_判定不能": "ライブの記録(index.htmlに実装済み)を、追加で蓄積する。件数が増えるまで、方向も確率も出さない",
}

# ---------- 使わないと決めたもの(探索の自由度を減らすため) ----------
NOT_DONE = [
    "パラメータの最適化(ゾーン幅・強度の重み・反発の定義など)。実運用の既定値のまま",
    "時間帯・曜日・レジーム別の切り口を、判定に使うこと(探索的分析としての報告は可)",
    "TP/SLの最適化",
    "成績を見てからの、切り口の追加・削除",
]

CONFIG = dict(DATA=DATA, DETECTOR=DETECTOR, JUDGE_EVERY=JUDGE_EVERY, COST_ONE_WAY_PIPS=COST_ONE_WAY_PIPS, COST_ROUND_PIPS=COST_ROUND_PIPS,
              COST_SENSITIVITY=COST_SENSITIVITY, TRADE_RULES=TRADE_RULES, ENTRY=ENTRY, HORIZON_BARS=HORIZON_BARS, EXIT_MODE=EXIT_MODE,
              AUX_TP_SL_ATR=AUX_TP_SL_ATR, PRIMARY_CELLS=PRIMARY_CELLS, N_TESTS=N_TESTS, INDEPENDENCE=INDEPENDENCE, CI_METHOD=CI_METHOD,
              YEAR_CONSISTENCY=YEAR_CONSISTENCY, SPLIT_HALVES=SPLIT_HALVES, PLACEBO=PLACEBO, VERDICTS=VERDICTS, ROUTES=ROUTES, NOT_DONE=NOT_DONE)

def config_hash():
    return hashlib.sha256(json.dumps(CONFIG, sort_keys=True, ensure_ascii=False, default=str).encode()).hexdigest()[:16]

if __name__ == "__main__":
    print("設定ハッシュ:", config_hash())
    print("主要な切り口:", N_TESTS, "個 / Bonferroni 有意水準:", round(0.05 / N_TESTS, 4))
